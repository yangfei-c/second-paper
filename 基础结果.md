# 基础结果

## 项目结构

项目由三个基础学习任务和一个目录生成任务组成：

> MTG-Jamendo 音乐标签、DEAM/PMEmo 音乐 VA、中英文文本 VA，以及使用已训练音乐模型生成 MTG Music Catalog。

```text
MSMMR/
├─ configs/
│  ├─ datasets.yaml          # 数据、划分、标注和预计算特征路径
│  ├─ music_training.yaml    # 音乐模型、损失、优化器与训练参数
│  ├─ text_training.yaml     # 文本模型、损失、优化器与训练参数
│  ├─ pipeline.yaml          # 全流程模式、验证列表与实验开关
│  └─ local_catalog.yaml     # 本机目录生成配置；不纳入 Git
├─ msmmr/
│  ├─ data/                  # 音乐/文本读取、清洗、划分、collate、DataLoader
│  ├─ models/                # MusicMultiTaskModel 与 XLMRobertaVAModel
│  ├─ experiments/           # 三类实验的装配与稳定任务注册表
│  ├─ training/              # 训练循环、任务规格、工厂、checkpoint、通用工具
│  ├─ inference/             # MTG Music Catalog 推理、验收与保存
│  ├─ losses.py              # ASL、单标签组加权 BCE、VA loss
│  └─ metrics.py             # MTG 与 VA 指标
├─ scripts/                  # 人工运行入口
├─ validation/               # 只读数据检查和轻量功能验证
└─ outputs/                  # 每次实验的配置、日志、checkpoint、指标和预测
```

## 推理结果

已生成的 `outputs/inference/mtg/mtg_music_catalog.npz` 含 55,525 首唯一歌曲：train/validation/test 为 32,859 / 11,101 / 11,565。字段与形状为：

| 字段 | 形状 |
|---|---:|
| `song_id`, `split` | $(55{,}525,)$ |
| `pred_va` | $(55{,}525,2)$ |
| `genre_prob` | $(55{,}525,87)$ |
| `instrument_prob` | $(55{,}525,40)$ |
| `mood_prob` | $(55{,}525,56)$ |
| `tag_prob` | $(55{,}525,183)$ |
| `va_embedding`, `tag_embedding` | 各 $(55{,}525,256)$ |

目录中的 Valence 预测范围为 $[-0.8474,0.7571]$，Arousal 预测范围为 $[-0.9388,0.9264]$，均为有限值且位于 $[-1,1]$。这里的两个 256 维音乐 embedding 分别来自 VA checkpoint 和标签 checkpoint，不应视为同一个已对齐空间。

---

## 音乐模型

输入为每首歌曲多个 MERT segment：

$$
X\in\mathbb{R}^{B\times S\times 12\times 768}.
$$

代码固定选择第 5、6 个 0-based hidden layer 索引：

$$
[B,S,12,768]
\rightarrow [B,S,2,768]
\rightarrow [B,S,1536].
$$

每个 segment 先映射到 256 维：

$$
h_{bs}=\operatorname{Dropout}\!\left(
\operatorname{GELU}\!\left(W\operatorname{LN}(x_{bs})+b\right)
\right)
\in\mathbb{R}^{256}.
$$

默认 `mean_std` pooling：

$$
\mu_b=\frac{\sum_s M_{bs}h_{bs}}{\sum_s M_{bs}},
$$

$$
\sigma_b=\sqrt{
\frac{\sum_sM_{bs}(h_{bs}-\mu_b)^2}{\sum_sM_{bs}}+\varepsilon
}.
$$

因此：

$$
[B,S,256]
\rightarrow [\mu;\sigma]\in\mathbb{R}^{B\times512}
\rightarrow z_{music}\in\mathbb{R}^{B\times256}.
$$

四个输出头为：

$$
z_{music}\rightarrow
\begin{cases}
\hat y_g\in\mathbb{R}^{87},\\
\hat y_i\in\mathbb{R}^{40},\\
\hat y_m\in\mathbb{R}^{56},\\
\hat y_{VA}\in\mathbb{R}^{2}.
\end{cases}
$$

前三组拼接成：

$$
87+40+56=183.
$$

## 文本模型

文本最大长度为 $L=128$：

$$
[B,L]_{ids},[B,L]_{mask}
\xrightarrow{\text{XLM-R base}}
[B,L,768]
\xrightarrow{\langle s\rangle}
[B,768]
\rightarrow[B,256]
\rightarrow[B,2].
$$

XLM-R 除最后 4 层外全部冻结。当前 256 维文本 embedding 与 256 维音乐 embedding **没有共享空间训练目标**，因此不能直接用二者的余弦相似度解释文本—音乐匹配。

---

## 数据与监督

### MTG-Jamendo

当前代码使用 official split-0 manifest：

$$
183=87\ \text{genre}+40\ \text{instrument}+56\ \text{mood/theme}.
$$

样本数为：

| 子任务 | train / validation / test |
|---|---:|
| full183 | 32859 / 11101 / 11565 |
| genre87 | 32572 / 11043 / 11479 |
| instrument40 | 14395 / 5466 / 5115 |
| mood56 | 9949 / 3802 / 4231 |

### DEAM / PMEmo

静态 VA 原始值按 $1$–$9$ 量表映射到 $[-1,1]$：

$$
y_{[-1,1]}=\frac{y_{[1,9]}-5}{4}.
$$

划分：

| 数据集 | train / validation / test |
|---|---:|
| DEAM | 1261 / 271 / 270 |
| PMEmo | 536 / 116 / 115 |

联合训练使用 `WeightedRandomSampler` 做**域平衡采样**，验证选模使用等域 CCC，而不是简单按样本数加权。

### 文本 VA

文本标签从 $[0,1]$ 映射到 $[-1,1]$：

$$
y_{[-1,1]}=2y_{[0,1]}-1.
$$

上传材料记录：去重后 19,761 条，train/validation/test 为 15,807 / 1,974 / 1,980。

---

## 当前损失函数

### full183：三组等权 ASL

$$
\mathcal L_{tag}
=\frac{1}{3}
\left(
\mathcal L_{genre}+\mathcal L_{instrument}+\mathcal L_{mood}
\right).
$$

当前配置：

$$
\gamma_+=0,\qquad \gamma_-=4,\qquad \text{clip}=0.05.
$$

### 单组标签：positive-weighted BCE

$$
w_c^+=\operatorname{clip}
\left(
\frac{N-n_c}{\max(n_c,1)},1,20
\right).
$$

### VA：Smooth L1 + CCC

$$
\operatorname{CCC}(\hat y,y)=
\frac{2\operatorname{Cov}(\hat y,y)}
{\operatorname{Var}(\hat y)+\operatorname{Var}(y)+(\mu_{\hat y}-\mu_y)^2}.
$$

当前配置：

$$
\mathcal L_{VA}=\frac12\sum_{k\in\{V,A\}}
\left[
\operatorname{SmoothL1}_{\beta=0.1}(\hat y_k,y_k)
+0.1\bigl(1-\operatorname{CCC}_k\bigr)
\right].
$$

---

## 测试结果

以下数值于 2026-08-10 从各运行目录的 `test_metrics.json` 读取复核；本次重构没有重新训练模型。

### MTG-Jamendo

| 模型 | genre mAP / AUC | instrument mAP / AUC | mood mAP / AUC | TagScore | overall mAP / AUC |
|---|---:|---:|---:|---:|---:|
| full183 | 0.1821 / 0.8596 | 0.1150 / 0.7570 | 0.0920 / 0.7863 | 0.1297 | 0.1399 / 0.8147 |
| genre87 | 0.1887 / 0.8692 | — | — | — | — |
| instrument40 | — | 0.2009 / 0.7707 | — | — | — |
| mood56 | — | — | 0.1435 / 0.7688 | — | — |

注意：单组任务使用的是不同过滤子集和不同 loss，不能由此推出“拆头必然优于 full183”。

### 音乐 VA

| 训练数据 | 测试域 | CCC-V / CCC-A | 平均 CCC | $R^2$-V / $R^2$-A | RMSE-V / RMSE-A |
|---|---|---:|---:|---:|---:|
| DEAM | DEAM | 0.7406 / 0.7737 | 0.7571 | 0.4806 / 0.4883 | 0.2142 / 0.2149 |
| PMEmo | PMEmo | 0.7409 / 0.8580 | 0.7995 | 0.5189 / 0.7176 | 0.2220 / 0.2049 |
| Joint | DEAM | 0.7028 / 0.7644 | 0.7336 | 0.4674 / 0.4936 | 0.2169 / 0.2138 |
| Joint | PMEmo | 0.7400 / 0.8756 | 0.8078 | 0.4886 / 0.7732 | 0.2288 / 0.1836 |
| Joint | pooled（样本加权） | 0.7458 / 0.8351 | 0.7905 | 0.5282 / 0.6598 | 0.2205 / 0.2052 |

joint checkpoint 的选模指标是 DEAM 与 PMEmo 两个域平均 CCC 的等权均值，即 $(0.7336+0.8078)/2=0.7707$；表中的 pooled 0.7905 是按样本合并后的分数，二者不可混用。联合训练对 PMEmo Arousal 有明显正向结果，但对 DEAM Valence、PMEmo Valence 并非统一改善，因此当前不能把 joint 写成“稳定域泛化”。

### 文本 VA

| 范围 | CCC-V / CCC-A | 平均 CCC |
|---|---:|---:|
| 全部测试集 | 0.8033 / 0.7416 | 0.7724 |
| 语言等权聚合 | — | 0.7201 |
| English | — | 0.7875 |
| Chinese | — | 0.6527 |
| CVAI | 0.4233 / 0.6789 | 0.5511 |
| CVAS | 0.7366 / 0.4001 | 0.5684 |
| CVAT | 0.8383 / 0.5193 | 0.6788 |
| EmoBank | 0.7256 / 0.3022 | 0.5139 |
| Facebook | 0.7571 / 0.8477 | 0.8024 |

模型 checkpoint 按 English 与 Chinese 的平均 CCC 等权选取，因此代码使用的文本选模指标是 0.7201，而不是样本加权的全测试集 0.7724。当前明显问题是中文整体较弱，以及 CVAS/EmoBank 的 Arousal 较弱。

---

## 与相关工作的关系

### MTG 标签

2025 年 *Music Tagging with Classifier Group Chains* 已明确研究 MTG-Jamendo 中 genre/instrument/mood 等标签组之间的依赖，因此“把 183 标签分组”本身不能作为新意。当前 full183 数值可作为基础结果，但正式论文仍需要统一指标实现、同 seed 预算或公开预测级复核后再讨论优劣。

### 音乐 VA

2025 年 *Towards Unified Music Emotion Recognition across Dimensional and Categorical Models* 已联合 MTG-Jamendo、DEAM、PMEmo 等数据并融合 categorical/dimensional supervision。当前仓库的 VA 结果是可用基础，但不是领先的 MER 新方法。

### 文本—音乐桥

ISMIR 2021 *Emotion Embedding Spaces for Matching Music to Stories* 已证明 VA 可以作为文本与音乐之间的低维情绪桥，因此“把文本 VA 和音乐 VA 放到同一二维坐标”只能作为系统设计，不能单独作为论文创新。
